# glacier
update 5/11: server2 and index2 are most up to date
taconite and coal shovel data base with SQL

# Imports
npm init -ynpm, 
install express sqlite3,
npm install csv-parser for csv file parsing
